{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Mac) Launch",
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "buildMac",
            "program": "${workspaceFolder}/bin/Debug/SensorNetworkApp",
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}